{
  "skill_name": "otel-config-validator",
  "evals": [
    {
      "id": 1,
      "eval_name": "broken-multi-issue",
      "prompt": "I had this otel collector config OOM in prod last night. Can you tell me everything that's wrong with it? The file is `broken_config.yaml` in the current directory.",
      "expected_output": "Structured report flagging at minimum: missing memory_limiter, batch placed before tail_sampling (wrong order), tls.insecure=true, otlp receiver bound to 127.0.0.1, deprecated 'logging' exporter (renamed to 'debug'), no sending_queue/retry on otlp exporter, tail_sampling single-instance constraint.",
      "files": ["broken_config.yaml"]
    },
    {
      "id": 2,
      "eval_name": "subtle-cardinality",
      "prompt": "My collector config passes `otelcol validate` but I'm hitting weird issues in prod: Grafana Mimir is exploding in cardinality, and my filelog replays the entire history on every restart. Can you audit `subtle_config.yaml` and tell me what's going on?",
      "expected_output": "Structured report flagging at minimum: high-cardinality attributes (request_id, user_id) added on a metrics pipeline, filelog start_at: beginning without file_storage extension, no service.telemetry block exposed.",
      "files": ["subtle_config.yaml"]
    },
    {
      "id": 3,
      "eval_name": "clean-prod-ready",
      "prompt": "Final audit before pushing to prod: please review my `clean_config.yaml`. It's for a contrib collector running as a gateway behind a load balancer.",
      "expected_output": "Validation report indicating the config is ✅ valid, with the list of detected components and their roles. Should NOT invent issues. May suggest minor optimizations but should clearly state no blocking errors.",
      "files": ["clean_config.yaml"]
    }
  ]
}
